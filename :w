use crate::forest::Forest;
mod forest;
// use crate::boosted::Forest;
mod boosted; // RAM * 2 but 2/3 times faster

fn main() {
    {
        {
            let mut forest2 = Forest::new(20000, 20000);
            forest2.create_alive_trees2(85);
        }
        // loop {
        //     forest.spread_fire();
        //     if forest.trees_last_burned_positions.is_empty() {
        //         break;
        //     }
        // }
    }
}
